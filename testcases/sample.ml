{
    int x;
    int y;
    int z;
    x = true;
    int if;
    bool flag;
    y = 20;
    z = x + y * 2;
    if = 5;
    flag = z > 40;

    if (flag) {
        print(z);
    } else {
        print(x);
    }

    while (x < y) {
        x = x + 1;
    }

    print(z);
}
